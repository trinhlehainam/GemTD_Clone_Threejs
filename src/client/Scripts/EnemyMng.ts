import {Vector2} from 'three'
import Enemy from './Enemy'

export default class EnemyMng{
    private enemies: Array<Enemy>

    constructor(){

    }

    spawnEnemies(paths: Array<Vector2[]>): void {

    }

    update(deltaTime_s: number): void {

    }
}
